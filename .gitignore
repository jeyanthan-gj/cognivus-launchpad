# ── Secrets — NEVER commit these ─────────────────────────────────────────────
.env
.env.*
!.env.example
.dev.vars
secrets/

# ── Build output ──────────────────────────────────────────────────────────────
node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro

# ── Runtime / framework cache ─────────────────────────────────────────────────
.wrangler/

# ── Logs — never commit log files (may contain PII or internal errors) ────────
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── OS / editor artifacts ─────────────────────────────────────────────────────
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Bun (project uses npm) ────────────────────────────────────────────────────
bun.lockb
bunfig.toml
